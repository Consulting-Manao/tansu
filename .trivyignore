# Trivy vulnerability ignore file
#
# The following CVEs cannot be fixed by updating project dependencies.
# They are Ruby gem (activesupport) vulnerabilities found in Gemfile.lock
# files bundled inside the published npm package @stellar/js-xdr
# (under node_modules). These are third-party Ruby dependencies that
# ship with the JavaScript npm package — we cannot update Ruby gems
# inside a published npm package.
#
# All other previously listed CVEs have been fixed by updating deps
# (esbuild ^0.28.1 via override, serialize-javascript ^7.0.3 via override,
# and the rest via npm update in website/).

# @stellar/js-xdr ships a Gemfile.lock with activesupport 6.1.7.6
# This is a Ruby gem used by the Ruby Stellar SDK, not our code.
CVE-2026-33176
