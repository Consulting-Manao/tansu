---
sidebar_position: 4
---

# Membership & Badges

The smart-contract powering Tansu keeps track of community members **on-chain** and assigns them _badges_. These badges are more than simple labels – they carry a **numerical weight** that is used for weighted voting in the default governance mode.

## Registering as a member

Anyone can register themselves on-chain by calling:

```bash
stellar contract invoke \
  --source-account G... \
  --network testnet \
  --id <TANSU_CONTRACT_ID> \
  -- \
  add_member \
  --member_address G... \
  --meta <ipfs_cid>
```

`meta` is optional. When supplied it must be an **IPFS CID** pointing to a folder that contains:

- `profile.json` – free-form JSON metadata (name, description, …)
- `profile-image.png` – avatar (optional)

The dApp will automatically render this information in the **Member profile** modal. `update_member` takes the same arguments to change them later.

## Binding a Git identity

A member can link their Stellar address to a Git identity such as `github:alice`, so that on-chain actions and commits can be attributed to the same person. `add_member` and `update_member` take three optional arguments, which must be given together:

- `git_identity` – the handle, e.g. `github:alice`
- `git_pubkey` – an Ed25519 SSH public key (32 bytes)
- `git_sig` – an Ed25519 signature (64 bytes) made with that key

The contract verifies the signature on-chain. It is an SSH signature (`SSHSIG`, namespace `tansu`, SHA-256) over:

```text
"Stellar Signed Message:\n" || member_address || git_pubkey || git_identity
```

Including the member address ties the proof to the account that authorized the call, so a signature cannot be replayed to bind the key to someone else's address. With OpenSSH this is `ssh-keygen -Y sign -n tansu -O hashalg=sha256`; the dApp's **Join** form prepares the message and gives the exact command.

The contract proves the member holds the private key; it does not know which forge account the key belongs to. The dApp covers that link for GitHub and GitLab by taking the public key from the SSH keys the account publishes on the forge. For other forges the member supplies the key, and the link rests on their word.

## Badges

Maintainers can assign one or several badges to a member for a given project. Badges are stored on-chain and influence the maximum weight a member's vote can carry in **badge-based** proposals.

| Badge     | Weight     | Typical use-case                |
| --------- | ---------- | ------------------------------- |
| Developer | 10 000 000 | Core code contributors          |
| Triage    | 5 000 000  | Issue triage & review           |
| Community | 1 000 000  | Active community member         |
| Verified  | 500 000    | A member which has been KYC/KYB |
| Default   | 1          | Anyone else                     |

> The weight column reflects the numeric values found in the `Badge` enum of the contract (see source code). When several badges are present their weights are **summed** to obtain the member's total voting power.

### Adding badges

Only maintainers of a project can add badges:

```bash
stellar contract invoke \
  --source-account <MAINTAINER_G...> \
  --network testnet \
  --id <TANSU_CONTRACT_ID> \
  -- \
  set_badges \
  --maintainer <MAINTAINER_G...> \
  --key <project_key> \
  --member <member_G...> \
  --badges '[Developer,Community]'
```

The dApp exposes a convenient **"Add badge"** modal that wraps this call.

## Voting weight modes

Effective vote weight depends on the proposal type and project configuration. The contract's `get_max_weight` helper applies to badge-based and NQG-weighted projects; token-based proposals use a different path at vote time.

| Mode | When it applies | How weight is determined |
| --- | --- | --- |
| **Badge-based** | Default; `create_proposal` with `token_contract: None` on a normal project | Sum of assigned badge enum values (default **1** if unregistered or no badges) |
| **Token-based** | `create_proposal` passes a `token_contract` | Voter chooses `weight` in whole token units (must not exceed balance); badge cap is skipped |
| **NQG contract** | Project key matches admin-configured `NqgProjectKey` (SCF Public Goods deployment) | `get_max_weight` calls the external NQG contract; returns **0** if score ≤ 4M (pilots only) |

### NQG contract weight

One designated project — the Stellar Community Fund Public Goods Award deployment — uses live Neural Quorum Governance scores instead of badges. The Tansu admin configures the NQG contract via `set_nqg_contract`. Voters below the 4M score threshold receive zero weight (pilots only).

This is also what makes the project Sybil-resistant without asking voters for collateral: weight is earned reputation, so new addresses carry none. See [Spam and Sybil resistance](./governance.mdx#spam-and-sybil-resistance).

The Stellar community membership itself (the soulbound token with a member's role, verified accounts and projects) is a separate contract, maintained in the [stellar-membership](https://radicle.network/nodes/radicle.consulting-manao.com/rad%3Az4KRDyBiL6kP6n5FWP6kJWga6BXJV) repository. It exposes the same NQG score per member through its `governance` function; Tansu only reads the NQG contract directly.

### Token-based proposals

When a proposal is created with a `token_contract`, voting weight is capped by the voter's token balance, not `get_max_weight`. Balances are not locked, so token weight is not Sybil-resistant: see [Spam and Sybil resistance](./governance.mdx#spam-and-sybil-resistance).

## Query helpers

Smart-contract helpers make it easy to retrieve information:

- `get_member` → full `Member` structure (projects + badges + meta)
- `get_badges` → badge holders for a project
- `get_max_weight` → badge sum or NQG score depending on project; token proposals bypass this at vote time

These endpoints are all used by the dApp and are public – anyone can call them from an RPC or another contract.
